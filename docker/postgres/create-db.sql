-- Note: The database "rmap" is already created by POSTGRES_DB environment variable
-- This script runs inside that database
CREATE DATABASE IF NOT EXISTS "rmap";
-- Create additional schemas if needed
CREATE SCHEMA IF NOT EXISTS "public";
